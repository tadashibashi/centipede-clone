if global.demo = false then score += 200; //increase score if not in demo mode

instance_create(x,y,explode_obj);
global_game_obj.bug_drop += global_game_obj.bug_cap - 1
b = round(ceil(random(4)))
if (b >= 0) && (b < 4) then {global_game_obj.bug_chance = true};

with (other) instance_destroy();
with (self) instance_destroy();



